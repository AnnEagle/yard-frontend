language: node_js
node_js:
  - "node"

cache:
  yarn: true
  directories:
    - "node_modules"

install:
  - yarn install
  - yarn flow-typed install

scripts:
  - lint
  - flow

deploy:
  provider: script
  skip_cleanup: true
  script:
    - echo "raz"
    - echo "dva"
    - echo "tri"
    # - yarn build
    # - now build --public --token $NOW_TOKEN --name yard-frontend
    # - now alias --token $NOW_TOKEN --force
  on:
    branch: feature-ci
